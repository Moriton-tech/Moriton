# 🐛 Үйлчилгээний үнэ устдаг асуудал — шалгалт ба засвар

**Хариулт: тийм, системийн алдаа байсан.** Танай ажилтнуудын буруу биш.
Гурван алдаа нийлж үнийг устгаж байсан.

---

## Алдаа 1 — Үнэ Firebase рүү ОГТ явдаггүй байсан ⛔ (үндсэн шалтгаан)

`saveServicePrices()` функц `saveAll()` буюу **зөвхөн localStorage** руу
хадгалаад "✅ Үнэ хадгалагдлаа" гэж мэдэгддэг байсан. Firestore руу бичих
код байгаагүй.

Өөрөөр хэлбэл үнэ нь **зөвхөн тухайн нэг компьютерийн браузерт** үлдэж,
сервэр дээр хуучин (эсвэл хоосон) хуулбар хэвээр байсан.

`addCustomService()` (шинэ үйлчилгээ нэмэх) болон `removeCustomService()`
мөн ижил алдаатай байсан.

## Алдаа 2 — Сервэрийн хуучин хуулбар локалыг дардаг байсан

`clinic_config` сонсогч ирсэн бүх snapshot дээр:

```js
STATE.servicePrices = docData.servicePrices;   // нөхцөлгүй дарж бичдэг
lsSet('mt_service_prices', STATE.servicePrices); // localStorage-д ч бичдэг
```

Firestore-ийн `onSnapshot` нь **хуудас ачаалах болгонд** одоогийн document-ийг
шууд илгээдэг. Тиймээс:

```
Админ үнэ оруулна  →  зөвхөн localStorage-д ороод
хуудсаа сэргээнэ   →  сервэрийн хуучин хуулбар ирж
                      локал үнийг УСТГАНА (localStorage-оос ч)
```

Энэ нь яг таны хэлсэн **"хадгалсан дүн удалгүй устаж арилсан"** байдал.

## Алдаа 3 — Сарын хуваарь хадгалахад тохиргоо бүхэлдээ устдаг байсан

```js
fbWriteDoc('clinic_config', 'main', { staffSchedule: ..., _updatedAt: ... });
```

Firestore-ийн `setDoc` нь **merge хийдэггүй — document-ийг бүтнээр СОЛИНО**.
Тиймээс хэн нэгэн сарын ажлын хуваарь хадгалах бүрд `servicePrices`,
`customServices`, `removedServices` сервэрээс бүрэн устаж байсан.

---

## Хийсэн засвар

**1. Нэгдсэн бичигч.** `fbSaveClinicConfig()` — тохиргоог **үргэлж бүтнээр**
бичнэ (үнэ + нэмэлт үйлчилгээ + устгасан үйлчилгээ + хуваарь + шинжилгээний
тохиргоо). Хэсэгчилсэн бичилт бүгд үүгээр солигдсон:

- `saveServicePrices()` — одоо Firebase рүү бичнэ ✅
- `addCustomService()` / `removeCustomService()` — мөн адил ✅
- `saveSchedule()` — тохиргоог устгахаа больсон ✅
- шинжилгээний тохиргоо, `fbForcePush`, offline retry — бүгд нэг замаар ✅

**2. Устгахаас хамгаалах шүүлт.** `clinic_config` ирэхэд:

| Нөхцөл | Урьд нь | Одоо |
|---|---|---|
| Сервэрийн хуулбар локалаас **хуучин** | дарж устгана | үл тоомсорлож, локалыг сервэрт **буцаан бичнэ** |
| Сервэр дээр `servicePrices` **огт алга** | — | локалыг сервэрт сэргээнэ |
| Сервэр **шинэ** | дарж бичнэ | дарж бичнэ (зөв — устгал ч дамжина) |
| Энэ төхөөрөмж тохиргоо бичиж үзээгүй (шилжилтийн үе) | дарж устгана | хоёр талыг **нэгтгэнэ**, дараа нь сервэрт нийлүүлнэ |

Үүнд `_updatedAt` timestamp харьцуулалт ашигласан. Ping-pong болохгүйг
тестээр баталгаажуулсан.

**3. 🕘 Түүхээс сэргээх товч.** "Үнэ оруулах" цонхонд шинэ товч. Өмнөх
үзлэг/хэвтэн эмчлүүлэлтэд ашигласан **хамгийн сүүлийн үнээр** хоосон
талбарыг дүүргэнэ. Одоо байгаа үнийг хөндөхгүй, автоматаар хадгалахгүй —
шалгаад "Хадгалах" дарна.

---

## Та юу хийх вэ

1. **Deploy хийнэ:** `firebase deploy --only hosting`
   (`sw.js`-ийн APP_VERSION → `2026-07-31-3` болсон тул автоматаар тархана)

2. **Үнэ оруулж байсан компьютер дээр** аппыг нээнэ. Хэрэв үнэ хэвээр
   харагдаж байвал — шинэ код нь тэрийг **автоматаар Firebase рүү нийлүүлнэ**.
   Баталгаатай болгохын тулд "Үнэ оруулах" → **💾 Хадгалах** дарж болно.

3. **Үнэ бүрэн алга болсон бол** → "Үнэ оруулах" → **🕘 Түүхээс сэргээх** →
   шалгаад **💾 Хадгалах**. Өмнө үзлэгт ашигласан үнэ бүгд нөхөгдөнө.

4. Дараа нь өөр компьютероос нэвтэрч үнэ зөв тархсаныг шалгана.

---

## Тест (Chromium, Firestore stub)

- ✅ Үнэ хадгалахад Firebase рүү бүтэн тохиргоо бичигдэнэ
- ✅ Сарын хуваарь хадгалахад үнэ хэвээр (хуучин алдаа давтагдахгүй)
- ✅ Шинжилгээний тохиргоо хадгалахад үнэ хэвээр
- ✅ Хуучин snapshot ирэхэд локал үнэ хадгалагдаж, сервэрт буцаан бичигдэнэ
- ✅ `servicePrices` талбаргүй snapshot ирэхэд ч устахгүй
- ✅ Сервэр жинхэнээсээ шинэ үед хэвийн шинэчлэгдэнэ
- ✅ Шилжилтийн үед хоёр талын үнэ нэгтгэгдэнэ (2 + 1 = 3)
- ✅ Нэгтгэсний дараа давхар бичилт хийхгүй (ping-pong алга)
- ✅ Түүхээс сэргээх: хамгийн сүүлийн үнийг зөв сонгож, байгаа үнийг хөндөхгүй
