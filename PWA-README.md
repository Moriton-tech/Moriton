# Морьтон — PWA (суулгаж болох вэб програм)

Апп одоо Chrome/Edge дээр **"Суулгах"** боломжтой болсон: Desktop дээр icon
үүсч, Start menu-д орж, браузерын хаягийн мөргүй тусдаа цонхонд нээгдэнэ.
Хэрэглэгчийн хувьд жинхэнэ Windows програмтай ижил харагдана — гэхдээ
шинэчлэлт автомат, суулгах багц татах шаардлагагүй.

---

## Шинээр нэмэгдсэн файлууд

| Файл | Үүрэг |
|---|---|
| `manifest.json` | Аппын нэр, өнгө, icon, standalone цонхны тохиргоо |
| `sw.js` | Service worker — суулгах боломж + автомат шинэчлэлт |
| `pwa.js` | SW бүртгэх, шинэчлэлт удирдах, "Суулгах" товч, офлайн заагч |
| `icons/` | 192/512 (энгийн + maskable), apple-touch-icon, favicon |
| `firebase.json` | Hosting тохиргоо + **кэшийн header** (маш чухал) |
| `.firebaserc` | Firebase project: `moriton-4ecc0` |

`index.html`-д хоёр л засвар орсон:
1. `<head>`-д manifest болон icon-ийн `<link>`-үүд
2. `</body>`-ийн өмнө `<script src="pwa.js">`

`app.js`, `style.css`, `firebase.js` **огт өөрчлөгдөөгүй.**

---

## Deploy хийх

```bash
# нэг удаа
npm install -g firebase-tools
firebase login

# төслийн хавтас дотроос
firebase deploy --only hosting
```

Дараа нь `https://moriton-4ecc0.web.app` дээр орж:
- **Chrome / Edge**: хаягийн мөрний баруун талд ⊕ (Install) тэмдэг гарна,
  эсвэл баруун доод буланд **"⤓ Програм болгож суулгах"** товч гарч ирнэ.
- **iPad / iPhone (Safari)**: Share → *Add to Home Screen*.
- Суулгасны дараа Desktop icon-оос шууд нээгдэнэ.

---

## ⚠️ Шинэ хувилбар тавих бүрд

`app.js`, `style.css`, `index.html` зэргийг өөрчилсөн бол **`sw.js` доторх
`APP_VERSION`-ийг заавал солино**:

```js
const APP_VERSION = '2026-07-31-1';   // → '2026-08-05-1'
```

Дараа нь `firebase deploy --only hosting`.

Юу болох вэ:
1. Нээлттэй байгаа апп минут тутам шинэ хувилбар шалгадаг
2. Шинэ `sw.js` олдвол шууд идэвхжинэ, хуучин кэш устана
3. Хуудас **автоматаар reload** хийж шинэ хувилбар руу шилжинэ

**Аюулгүйн хамгаалалт:** хэрэглэгч талбар бөглөж байх (input/textarea/select
фокустай) эсвэл модал/drawer нээлттэй бол reload-ыг **хойшлуулж**, 4 секунд
тутам дахин шалгана. Ажил дундуур гэнэт reload хийж өгөгдөл алдахгүй.

---

## Юуг ХӨНДӨӨГҮЙ вэ

Service worker нь Firestore, Auth, Storage-ийн дуудлагыг **огт таслан авахгүй**
(`sw.js` доторх `LIVE_HOSTS` жагсаалт). Real-time sync урьдын адил ажиллана.

**Өгөгдлийн оффлайн кэш ОРООГҮЙ** — таны сонголтоор. Интернэт тасарвал:
- Апп **нээгдэнэ** (shell кэшлэгдсэн), дээд талд улбар шар анхааруулга гарна
- Гэхдээ өвчтөний дата ирэхгүй, шинэ бичилт хадгалагдахгүй

Дараа нь бүрэн оффлайн болгох бол `firebase.js`-д Firestore-ийн
`persistentLocalCache` нэмэх хэрэгтэй (~30 мөр).

---

## Хассан бол болох зүйлс

`pwa.js` доторх сонголтууд:

- **Суулгах товч хэрэггүй бол** — `beforeinstallprompt` блокыг устгана
  (Chrome-ын хаягийн мөрний Install тэмдэг хэвээр үлдэнэ)
- **Офлайн анхааруулга хэрэггүй бол** — `initNetIndicator` дуудлагыг устгана
- **Автомат reload биш, товч дарж шинэчлэх бол** — `reloadWhenIdle()`-ийн
  оронд өөрийн `toast()`-оор мэдэгдэл гаргаж, товч дээр `location.reload()`

---

## Шалгасан зүйлс

Chromium дээр локал тест хийж баталгаажуулсан:

- ✅ Service worker бүртгэгдэж, app shell 13 файлыг кэшилнэ
- ✅ `manifest.json` зөв уншигдана (нэр, icon, standalone)
- ✅ Анхны суулгалт дээр **шаардлагагүй reload хийхгүй**
- ✅ Интернэтгүй үед апп нээгдэж, офлайн анхааруулга гарна
- ✅ Шинэ `APP_VERSION` тавихад: талбар фокустай үед reload **хойшилно**,
  фокус алдмагц автоматаар шинэчилнэ
- ✅ Хуучин кэш автоматаар устана
